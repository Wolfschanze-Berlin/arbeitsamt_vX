"use client";

// The SSH terminal UI is rendered in DashboardShell (layout) so it persists
// across page navigations. This page exists only so the /ssh route is valid
// and triggers the SSH view visibility in the shell.
export default function SSHPage() {
  return null;
}
